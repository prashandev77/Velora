/** Official logo file in /public — single source of truth (footer, admin, emails). */
export const AVELORA_LOGO_FILENAME = 'Avelora Travel Updated 5.0.svg';

/** Path for `next/image` `src` and absolute URLs (spaces encoded). */
export const AVELORA_LOGO_PATH = `/${encodeURIComponent(AVELORA_LOGO_FILENAME)}`;
